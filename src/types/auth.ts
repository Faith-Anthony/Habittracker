// Phase 2: Type definitions for auth
// Will implement authentication types in future phases

export interface User {
  id: string;
  email: string;
  name: string;
}

export interface AuthState {
  user: User | null;
  isLoading: boolean;
  error: string | null;
}
